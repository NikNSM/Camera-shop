export enum AddresesRoute {
  CATALOG = '/',
  CAMERA = '/camera/',
}

export enum ApiRoute {
  CAMERAS_LIST = '/cameras',
  PROMO = '/promo',
  ORDERS = '/orders'
}

export enum NameSpaceState {
  PRODUCT = 'product',
}
